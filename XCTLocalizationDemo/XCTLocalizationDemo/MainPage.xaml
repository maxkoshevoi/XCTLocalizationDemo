<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resx="clr-namespace:XCTLocalizationDemo.Resx"
             xmlns:vm="clr-namespace:XCTLocalizationDemo"
             x:Class="XCTLocalizationDemo.MainPage">

    <ContentPage.BindingContext>
        <vm:MainViewModel />
    </ContentPage.BindingContext>
    
    <StackLayout Spacing="20">
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="{x:Static resx:AppResources.Welcome}" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label Text="{x:Static resx:AppResources.AppDescription}" FontSize="16" Padding="30,0,30,0"/>
        <Button Text="{x:Static resx:AppResources.ChangeLanguage}" Command="{Binding ChangeLanguageCommand}" />
        <Label Text="{Binding Version}" FontSize="16" HorizontalTextAlignment="Center"/>
    </StackLayout>

</ContentPage>
